name: Build Android APK
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Web Project
        run: npm run build

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21' # تم التحديث هنا لحل خطأ invalid source release
          distribution: 'zulu'

      - name: Force Android Rebuild
        run: |
          rm -rf android
          npx cap add android
          npx cap sync

      - name: Android Build
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: THATTIA-PI-FINAL-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
