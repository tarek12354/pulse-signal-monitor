name: Build Android APK
on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install Dependencies
        run: npm install

      - name: Build Web Project
        run: npm run build

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'zulu'

      - name: Initialize Android Platform
        run: |
          rm -rf android
          # إنشاء المنصة بمعرف صالح لا يحتوي على أرقام مخالفة أو شرطات
          npx cap add android
          npx cap init THATTIA-PI com.thattiapi.app --web-dir dist
          npx cap sync

      - name: Android Build
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: THATTIA-PI-Final-APK
          path: android/app/build/outputs/apk/debug/app-debug.apk
